Goal: Build scrapers for Reddit (PRAW) and Twitter/YouTube RSS feeds.

Constraints: Implement a rate-limiter (max 15-20 posts per "Scout" run to save API costs).

Schema: Define the RawFeedback table (Source, Content, Timestamp, Author).